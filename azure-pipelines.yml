trigger:
- main

pool:
  vmImage: 'windows-2022'
  demands:
  - msbuild
  - visualstudio

variables:
  solution: 'Yeetnite Launcher.sln'
  buildPlatform: 'x64'
  buildConfiguration: 'Release'
  stagingDirectory: 'packages'
  signingCertificate: 'yeetnite-team.pfx'
  msbuildOutputDirectory: 'x64\Release\YeetniteLauncher'
  packagesDirectory: 'packages'

steps:
- task: NuGetCommand@2
  displayName: 'Install Yeetnite Launcher Dependencies'
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    vsVersion: '17.0'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
    clean: true
    msbuildArchitecture: 'x64'

- task: MsixPackaging@1
  inputs:
    outputPath: '$(packagesDirectory)\Yeetnite Launcher.msix'
    buildSolution: false
    inputDirectory: '$(msbuildOutputDirectory)'
    generateBundle: false
    updateAppVersion: false

- task: MsixSigning@1
  inputs:
    package: '$(packagesDirectory)\Yeetnite Launcher.msix'
    certificate: '$(signingCertificate)'
    passwordVariable: 'signingCertificatePassword'